{% include 'default/_form.html.twig' with {
    page_title: 'Delete Category'|trans({'%id%': category.id|default('')}),
    form: form,
    submit_label: 'Delete'|trans,
    back_to_list_path: 'category_index',
} %}
