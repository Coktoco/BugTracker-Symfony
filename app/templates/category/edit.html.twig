{% include 'default/_form.html.twig' with {
    page_title: 'Edit Category'|trans({'%id%': category.id|default('')}),
    form: form,
    submit_label: 'Submit Changes'|trans,
    back_to_list_path: 'category_index',
} %}