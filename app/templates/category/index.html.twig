{% extends 'base.html.twig' %}

{% block title %}
    {{ 'Bug-Trackerr Category List'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'Category List'|trans }}</h1>
    {% if pagination is defined and pagination.items|length %}
        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'Category_ID'|trans, 'Category_ID') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'Title'|trans, 'Title') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'Added'|trans, 'Added') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'Updated'|trans, 'Updated') }}</th>
                <th>{{ 'Buttons'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for category in pagination.items %}
                <tr>
                    <td>{{ category.id }}</td>
                    <td>{{ category.title }}</td>
                    <td>{{ category.createdAt|format_date('long') }}</td>
                    <td>{{ category.updatedAt|format_date('long') }}</td>
                    <td class="row">
                        <div class="col">
                            <a class="btn btn-success" href="{{ url('category_show', {id: category.id}) }}" title="{{ 'Details'|trans }}">
                                {{ 'Details'|trans }}
                            </a>
                        </div>
                        <div class="col">
                            <a class="btn btn-primary" href="{{ url('category_edit', {id: category.id}) }}" title="{{ 'Edit'|trans }}">
                                {{ 'Edit'|trans }}
                            </a>
                        </div>

                        <div class="col">
                            <a class="btn btn-danger" href="{{ url('category_delete', {id: category.id}) }}" title="{{ 'Delete'|trans }}">
                                {{ 'Delete'|trans }}
                            </a>
                        </div>

                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center row">
            <div class="col-4">
            {{ knp_pagination_render(pagination) }}
            </div>
            <div class="col-2">
                <a class="btn btn-primary" href="{{ url('category_create')}}" title="{{ 'New Category'|trans }}">
                    {{ 'New Category'|trans }}
                </a>
            </div>
            <div class="col-2">
                <a class="btn btn-primary ml-5" href="{{ url('task_index')}}" title="{{ 'Bugs List'|trans }}">
                    {{ 'Bugs List'|trans }}
                </a>
            </div>
        </div>

    {% else %}
        <p>
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}