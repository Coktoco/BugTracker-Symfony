{% include 'default/_form.html.twig' with {
    page_title: 'Edit Bug Report'|trans({'%id%': task.id|default('')}),
    form: form,
    submit_label: 'Submit Changes'|trans,
    back_to_list_path: 'task_index',
} %}