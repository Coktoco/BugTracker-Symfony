{% include 'default/_form.html.twig' with {
    page_title: 'label.edit_bug'|trans({'%id%': task.id|default('')}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'task_index',
} %}