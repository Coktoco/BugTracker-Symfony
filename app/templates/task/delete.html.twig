{% include 'default/_form.html.twig' with {
    page_title: 'Delete Bug Report'|trans({'%id%': task.id|default('')}),
    form: form,
    submit_label: 'Delete'|trans,
    back_to_list_path: 'task_index',
} %}
